<%- partial('_partial/head') %>

<% if (is_post()){ %>
<body data-spy="scroll" data-target="#toc" data-offset="50">
<% } else {%>
<body>
<% } %>

  <%- partial('_partial/header-post', null, {cache: !config.relative_link}) %>

  <div id="container">
    <div id="wrap">
            
      <div id="content" class="outer">
        <% if (theme.sidebar && theme.sidebar !== 'bottom' && !is_post()){ %>
          <section id="main"><%- body %></section>
          <%- partial('_partial/sidebar', null, {cache: !config.relative_link}) %>

        <% } else {%>
          <section id="main"><%- body %></section>
        <% } %>

        <!-- Table of Contents -->
        <% if (is_post()){ %>
        <aside id="sidebar">
          <div id="toc" class="toc-article">
            <div class="toc-title widget-title"><%= __('contents') %></div>
            <% if (toc(page.content) != ""){ %>
              <%- toc(page.content, { "class": "nav", "list_number": false }) %>
            <% } else { %>
            <ul class="nav"><%= __('none') %></ul>
            <% } %>
          </div>
        </aside>
        <% } %>

      </div>
    </div>
    <%- partial('_partial/footer', null, {cache: !config.relative_link}) %>
    <%- partial('_partial/after-footer') %>
  </div>

  <a id="rocket" href="#top" class=""></a>
  <script type="text/javascript" src="/js/totop.js" async=""></script>
</body>
</html>
